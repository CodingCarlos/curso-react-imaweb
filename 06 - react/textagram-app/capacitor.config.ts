import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.textagram123.app',
  appName: 'textagram',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
